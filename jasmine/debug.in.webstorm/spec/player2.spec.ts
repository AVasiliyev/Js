describe('Test 2', () => {

  beforeEach(() => {
    return;
  });

  it('positive case', () => {
    expect(true).toEqual(true);
  });

  it('negative case', () => {
      expect(true).toEqual(false);
  });
});

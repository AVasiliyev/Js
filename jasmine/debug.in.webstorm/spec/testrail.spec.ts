describe('Test Rail Tests', () => {

  beforeEach(() => {
    return;
  });

  it('[17661459] Base test - positive', () => {
    expect(true).toEqual(true);
  });

  it('[17661460] Base test - negative', () => {
      expect(true).toEqual(false);
  });
});
